# HTS

Monorepo containing three related Java/Spring Boot projects:

- **DashboardAuthenticate** — authentication dashboard service
- **HipsReport** — HIPS reporting application
- **HumeDashboard** — Hume dashboard application

Each project is a standalone Maven application with its own `pom.xml`.
